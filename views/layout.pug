doctype html
html
	head
		title= title
		link(rel='stylesheet', href='/stylesheets/style.css')
	body
		nav
			a(href='/') Home
			a(href='/new-message') Post a Message
			if !currentUser
				a(href='/sign-up') Sign Up
				a(href='/log-in') Log In
			else
				a(href=`/member/${currentUser.username}`) Profile
				a(href='/log-out') Log Out
		block content
